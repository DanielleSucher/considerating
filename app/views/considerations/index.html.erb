<div id="search-page"></div>
<div class="digg_pagination row"><%= will_paginate @all_considerations %></div>
<div id="all-considerations">
	<% @all_considerations.each do |consideration| %>
 		<%= render consideration %>
	<% end %>
</div>
<div class="digg_pagination row"><%= will_paginate @all_considerations %></div>