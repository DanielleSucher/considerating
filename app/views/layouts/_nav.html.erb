			<div class="row show-grid">
				<div class="span10 offset6" id="user_nav">
					<% if current_user %>
						<%= "Welcome #{current_user.name}!" %>
						<%= link_to "Sign out", signout_path %>
					<% else %>
						<%= link_to "!Sign in with Twitter", "/auth/twitter" %>
					<% end %>
				</div>
			</div>