<% if session[:user_id] != nil && current_user.voteds?(@consideration) != nil %>
 	<%= render 'shared/results' %>
<% else %>
	<%= render 'users/vote' %>
<% end %>
<%= render 'considerations/destroy_consideration' %>
